# GPT Model Configuration for Character-level Tokenization
# Format: PARAMETER_NAME=value

# Tokenizer configuration
# Options: char, word, sentencepiece
TOKENIZER_TYPE=char
# Vocabulary size (only used for word and sentencepiece tokenizers)
# For char tokenizer, vocab size is determined automatically from text
VOCAB_SIZE=2000

# Model architecture parameters
D_MODEL=128
CONTEXT_LEN=512
ATTENTION_HEADS=8
DECODER_BLOCKS=3
DROPOUT_RATE=0.1

# Training parameters
LEARNING_RATE=0.00001
EPOCHS=20
STEPS_PER_EPOCH=1000
WARMUP_RATIO=0.1
PEAK_LEARNING_RATE=1e-4
MIN_LEARNING_RATE=1e-6
