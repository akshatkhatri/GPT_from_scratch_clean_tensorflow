# GPT Model Configuration for Small Test
# Format: PARAMETER_NAME=value

# Tokenizer configuration
# Options: char, word, sentencepiece
TOKENIZER_TYPE=char
# Vocabulary size (only used for word and sentencepiece tokenizers)
# For char tokenizer, vocab size is determined automatically from text
VOCAB_SIZE=2000

# Model architecture parameters (small for testing)
D_MODEL=64
CONTEXT_LEN=128
ATTENTION_HEADS=4
DECODER_BLOCKS=2
DROPOUT_RATE=0.1

# Training parameters (optimized for small test)
LEARNING_RATE=0.001
EPOCHS=10
STEPS_PER_EPOCH=50
WARMUP_RATIO=0.1
PEAK_LEARNING_RATE=0.01
MIN_LEARNING_RATE=1e-5
